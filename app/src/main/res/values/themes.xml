<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="XPopup.Compat.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

    </style>

    <!-- 自定义弹框动画 -->
    <style name="PopupEnterRightExitLeftAnimation">
        <item name="android:windowEnterAnimation">@anim/in_from_right</item>
        <item name="android:windowExitAnimation">@anim/out_to_left</item>
    </style>

    <!--  项目自定义DialogActivity主题，继承XPopupDialogActivityTheme，重写相关属性  -->
    <style name="MyDialogActivityTheme" parent="XPopupDialogActivityTheme">
        <item name="android:backgroundDimAmount">0.6</item>
        <item name="android:windowAnimationStyle">@style/ActivityEnterRightExitLeftAnimation</item>
    </style>

    <!-- 项目自定义DialogActivity动画 -->
    <style name="ActivityEnterRightExitLeftAnimation">
        <item name="android:activityOpenEnterAnimation">@anim/in_from_right</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@anim/out_to_left</item>
    </style>


    <style name="PopupEnterExpandExitShrinkAnimation">
        <item name="android:windowEnterAnimation">@anim/enter_folds_expand</item>
        <item name="android:windowExitAnimation">@anim/exit_folds_shrink</item>
    </style>

</resources>